{
  "comment": "Provenance chain for the ACP schema snapshots in this directory. The drift workflow re-derives this chain from upstream and diffs every link. hermes-agent speaks ACP through the Python SDK, which generates its models from the Zed schema repo's UNSTABLE variants — so those are the snapshots we track.",
  "hermes_agent": {
    "repo": "https://github.com/NousResearch/hermes-agent",
    "version": "0.20.0",
    "acp_entrypoint": "hermes acp (acp_adapter.entry, JSON-RPC over stdio)",
    "python_sdk_pin": "agent-client-protocol==0.9.0"
  },
  "python_sdk": {
    "repo": "https://github.com/agentclientprotocol/python-sdk",
    "version": "0.9.0",
    "schema_ref": "refs/tags/v0.11.2",
    "generates_from": "schema.unstable.json + meta.unstable.json",
    "protocol_version": 1
  },
  "schema_source": {
    "repo": "https://github.com/zed-industries/agent-client-protocol",
    "tag": "v0.11.2",
    "files": [
      "schema/schema.unstable.json",
      "schema/meta.unstable.json"
    ]
  }
}
